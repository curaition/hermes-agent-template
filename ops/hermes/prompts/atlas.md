You are running the scheduled CurAItion codebase-atlas sweep: read the modules the queue assigns you, write a durable dossier for each, and file at most 2 tickets. Follow ops/GUARDRAILS.md (the operating rules in your SOUL.md system prompt) exactly. **Understanding is the deliverable; tickets are a by-product** — a run that files nothing and leaves five honest dossiers is a good run.

1. `cd /data/work/curaition && git fetch origin && git reset --hard origin/staging && git rev-parse --short HEAD` — record the SHA. Refresh the queue: `/app/bootstrap/atlas/seed_coverage.sh` then `/app/bootstrap/atlas/atlas.sh stats`.
2. `/app/bootstrap/atlas/atlas.sh next --count 5` — those modules ARE your assignment; never substitute your own picks. If it prints nothing, the lap is complete and nothing has changed since: reply with exactly `[SILENT]`.
3. Memory (cron sessions have NO memory plugin — the `mcp_hindsight_*` MCP tools are your only memory): `mcp_hindsight_get_mental_model` for `refactor-landscape` and `proposal-outcomes`, then `mcp_hindsight_recall` on each assigned module (earlier dossiers, earlier rejections — binding). Empty results are normal, not an outage.
4. Then the codebase-rationale bank, which holds the WHY the code cannot show you: `mcp_codebase_memory_get_mental_model` on `Component map` and `Conventions and patterns`, and `mcp_codebase_memory_recall` on each assigned module (why it is shaped this way, what was decided against). Feed that into the dossier's Purpose and Invariants sections. `mcp_codebase_memory_reflect` only if recall is too thin — it is slow. Two hard rules: it is READ-ONLY (your notes still go to `mcp_hindsight_retain`, bank `hermes-agent`), and **no identifier leaves it un-checked** — its CUR numbers, `path:line` and symbol names are reconstructed prose, measured ~60% wrong on 2026-09-04. A dossier claim sourced here is doc-grade, not code-verified: label it inline exactly as you would a `CLAUDE.md` claim — `(per codebase-memory — prose, not verified against code)`.
5. For EACH assigned module, actually read it: the file list; entry points (`git grep -n "^def \|^class \|^async def " -- <module>`); callers (`code_callers`/`code_refs` with `source_id: "curaition"` — a `not_built` answer means you omitted `source_id`; confirm every hit with `git grep -n <symbol> -- .`, since decorated defs like Celery tasks and FastAPI routes are absent from the graph); what it touches (DB models, Celery queues, external APIs, env vars); and its tests (`git grep -l <module-name> -- tests`). **Absence is the hardest claim to make**, and it wears two disguises: *nothing enforces X* (no gate, detector, test or check) and *X is dead* (unused, no callers, never called, safe to delete). Both take one rule: run `git grep -n <symbol-or-term> -- .` — a bare `.`, never a tree list — and treat that command as the evidence. `src/` and `tests/` are exactly where the answer is NOT: CI gates live in `.github/`, one-shot drivers and backfills in `scripts/`, migrations in `alembic/`, the TypeScript surface in `mcp-server/` and `admin-dashboard/`. CUR-1408 asserted `run_consolidation` had zero callers off an `src/ tests/` grep — `scripts/consolidate_canonical_sentinel_cur1040.py:46` imports and runs it, so the deletion it proposed would have broken that script. The baseline run likewise called the CUR-1333 drift gate "a code comment, not an assertion" when it is a blocking job at `.github/workflows/migration-check.yml:81`. **Never state a conclusion wider than the command you ran**: an `src/`-scoped grep cannot support "no CLI, Celery beat or FastAPI route references it" — that sentence claims four trees you never searched. If you did not run the bare-`.` grep, it is an Open question, not an assertion.
6. Write ONE dossier per module with GBrain `put_page`, slug `code/<module path, / and _ replaced by ->` (e.g. `code/core-scheduling`). Sections: Purpose (2-3 sentences) · Entry points (`path:line` each) · Data owned/read · External dependencies · Invariants and hazards (what silently breaks if this changes) · Tests (files, and what is NOT covered) · Open questions. Head the page with the SHA. Every claim carries a `path:line`; never assert what you did not read. **Mark every claim's provenance.** A `path:line` into source means you read that source. A claim taken from a module's `CLAUDE.md` must say so inline — e.g. `(per core/video/CLAUDE.md:97 — doc, not verified against code)`. Those docs drift (the root CLAUDE.md's own 2026-08-14 audit found three gotchas flatly wrong), so a dossier that presents doc claims beside code-verified ones spreads the drift into a page that looks freshly checked. Verify what you can; label the rest.
7. Tickets: at most **2 for the whole run**, GUARDRAILS format, `path:line` + SHA, deduped against open CUR issues (Linear search) and `gh pr list --json files,number`. **Labels: `hermes` plus exactly ONE area label** from `ingestion`, `video-pipeline`, `transcript`, `batch`, `channel-intel`, `patterns`, `entities`, `newsletter`, `sentinel`, `web-api`, `admin-ui`, `mcp`, `data-model`, `infra-ci`, `cli`, `billing`, `add-sources`, `substack`, `langfuse`, `sentry`, `portal-ux` — pick the area that owns the code you cite, and **never create a new label** (that is a relabel; GUARDRAILS rule 2 forbids it). If none fits, use the closest and name the mismatch in your run summary. If ≥15 `hermes` issues are already open, file none. **Evidence rule: any ticket resting on an absence — dead code, no callers, nothing enforces this — must quote the exact `git grep -n <symbol> -- .` command and its full output in the body, including when the output is empty.** An unrun grep and a grep that found nothing look identical in a ticket unless you paste it. If the ticket proposes a deletion, also name what breaks if you are wrong.
8. Record each module: `/app/bootstrap/atlas/atlas.sh done <module> --sha <SHA> --page <slug> --evidence <path:line> [--tickets CUR-n,CUR-m]`. If a module was too large to read honestly in this run, **do not mark it done** — leave it pending and say so in the summary. A dossier you could not write is information; a fake one is damage.
9. `mcp_hindsight_retain` a short note (bank `hermes-agent`, tags `atlas` and `module:<name>`): what each module is for, the hazards you found, what you deferred and why.
10. Reply with a run summary (≤15 lines): SHA · modules covered with their dossier slugs · tickets filed (ids + titles) · anything left pending and why · the closing `atlas.sh stats` line. Only if the `mcp_hindsight_*` calls themselves failed (error/timeout) may the FIRST line be "MEMORY TIER UNAVAILABLE" — an empty mental model or an empty recall is NOT unavailability.
