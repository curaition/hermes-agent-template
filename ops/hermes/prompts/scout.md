You are running the scheduled CurAItion issue-scout pass. Follow ops/GUARDRAILS.md (the operating rules in your SOUL.md system prompt) exactly.

Procedure:
1. `cd /data/work/curaition && git fetch origin && git reset --hard origin/staging && git rev-parse --short HEAD` — record the SHA.
2. Read your mental models via the Hindsight MCP tools: `mcp_hindsight_get_mental_model` with `{"mental_model_id": "refactor-landscape"}` and `{"mental_model_id": "proposal-outcomes"}` (Hindsight — NOT GBrain; cron sessions have no memory plugin, only these MCP tools). Then `mcp_hindsight_reflect` on 2–3 candidate areas you intend to look at; prior rejections and feedback are binding.
3. Read the codebase-rationale bank before judging a candidate: `mcp_codebase_memory_list_mental_models`, then `mcp_codebase_memory_get_mental_model` on the pages matching your areas (`Conventions and patterns`, `Key decisions and rationale`, `Component map`, `Core concepts`, `Initiatives and enhancements`). Use `mcp_codebase_memory_reflect` only when a page is too shallow for the WHY — it is agentic synthesis and slow. This bank is written by whoever is actually changing the code, so it will often tell you a change was already decided against, or that a rule you are about to flag exists for a measured reason. **Never carry an identifier out of it** — CUR numbers, `path:line`, symbol names there are reconstructed prose, measured ~60% wrong on 2026-09-04 (one fabricated CUR reached a real issue); confirm each in Linear or with `git grep -n <symbol> -- .` before it enters a ticket. You have no write tool here by design — your own learnings still go to `mcp_hindsight_retain` (bank `hermes-agent`).
4. For each candidate: pull GBrain knowledge (`query`, `get_page`), verify against the clone (`path:line`, and `git grep -n <symbol> -- src tests scripts alembic` — NEVER `src/` alone: migration drivers in `scripts/` are real callers), check blast radius (`code_callers`/`code_refs` with `source_id: "curaition"`, then confirm with `git grep -n` — decorated defs such as Celery tasks and routes are missing from the graph), dedupe against open CUR issues (Linear search), and check `gh pr list --json files,number` for in-flight overlap.
5. File at most 3 issues (labels `hermes` **plus exactly one area label** from `ingestion`, `video-pipeline`, `transcript`, `batch`, `channel-intel`, `patterns`, `entities`, `newsletter`, `sentinel`, `web-api`, `admin-ui`, `mcp`, `data-model`, `infra-ci`, `cli`, `billing`, `add-sources`, `substack`, `langfuse`, `sentry`, `portal-ux` — never invent one; format per GUARDRAILS, cite `path:line` + SHA). If ≥15 `hermes` issues are open, file none — consolidate/comment instead.
6. `mcp_hindsight_retain` a short outcome note (bank `hermes-agent`): what you proposed, what you skipped and why, anything surprising.
7. Reply with a run summary (≤15 lines): SHA, candidates considered, issues filed (ids + titles), skips with reasons. Only if the `mcp_hindsight_*` calls themselves failed (error/timeout) may the FIRST line be "MEMORY TIER UNAVAILABLE" — an empty mental model or an empty recall is NOT unavailability.
